# 2026-09-05 — t1–40 run launched; thread-page skill designed

**Author**: omp agent session with Matt

## What happened

- **Pushed**: `master` and `feature/v2-foundation` to origin (full history).
- **Run launched** (detached process `run-t1-40`): threads 1–40 in chronological OP-time order (IDs recorded in `data/exports/threads-first-40.json`), fresh `data/annotator-t1-40.db`, pass-id `t1-40`, kimi-k2.5, recording to `data/recordings/t1-40.jsonl`. Bounded supervisor `scripts/run-t40.sh` (committed): at most 3 failed attempts total (repo stop condition), 30-min backoff between attempts (quota windows), each attempt resumes from the `run_state` checkpoint.
- **Thread-synopsis wiki skill designed**: `docs/design/thread-pages.md` — raw DokuWiki format pinned from the newsletter raw sources (threads 38, 40), deterministic renderer + LLM teaser/summary, evaluation plan against published pages with the spoiler/future-knowledge caveat (published pages contain meta-knowledge a first-read artifact can't have; compare only on what was knowable at thread end).

## Monitoring the run

- Progress: `sqlite3 "file:data/annotator-t1-40.db?mode=ro" "SELECT thread_id, batch_index FROM run_state;"` (and `SELECT COUNT(*) FROM story_log`).
- Supervisor log: `data/recordings/t1-40.log`.
- If the process is gone and the log shows 3 failures: fix the cause, then relaunch `scripts/run-t40.sh` (fresh 3-attempt budget). The currently-running instance uses the identical `/tmp/run-t40.sh` copy — do not edit it mid-run.

## Next

- When the run completes: `terrarium-annotator verify --corpus-db banished.db --annotator-db data/annotator-t1-40.db`, quality report per the smoke-report shape, gold-set recall on the full 40-thread output.

## 2026-09-05 later — full-quest run launched

- Prompt revised (commit `01e19d0`): OR-form admission criteria, named-humans/books/taxonomy/description-entities callouts, full-restate update rule (fixes the "Revision:" narrowing), conflicting-accounts instruction.
- **Full-quest run live** (detached `run-full`, supervisor `scripts/run-full.sh`, committed): all 276 threads, fresh `data/annotator-full.db`, pass-id `full-v1`, kimi-k2.5, recordings `data/recordings/full-v1.jsonl`. Same bounded supervisor policy (max 3 failures, 30-min backoff, checkpoint resume). Expected duration: days across 5h quota windows.

- Then: thread-page skill (render + LLM summary) or researcher/critic tier, Matt's call.

## 2026-09-05 03:40 — attempt-1 diagnosis: FTS apostrophe bug (not quota)

Attempt 1 died at thread 5 batch 12: model called `search_glossary` with an apostrophe query and `GlossaryStore.search` interpolated it raw into FTS5 `MATCH` (`fts5: syntax error near "'`). Fixed before attempt 2 consumed the budget: phrase-escaped queries + dispatcher catches `sqlite3.OperationalError` + 4 regression tests (commit 41dcdd4). Attempt 2 resumed on schedule 03:38:58 with the fix; supervisor healthy (bounded at 3 failures).

## 2026-09-05 04:15 — filtered-restart recovery

Attempt 2 exposed a design bug: `only_threads` passes ignored checkpoints entirely ("always fresh"), so the supervisor resume RESTARTED from thread 1 and overwrote the valid checkpoint (30305969:4). 9 duplicate story gists from the reprocessing remain (harmless; duplicate entry proposals were rejected by DuplicateEntry). Fix (483bf60): a filtered pass now honors its own pass checkpoint when the checkpoint thread is inside the filter; out-of-filter checkpoints still mean a fresh start (tests updated). Checkpoint restored to last-good (30459080:12) via save_run_state (state API, not a hand edit). Run relaunched as run-t1-40-v2 (fresh 3-failure budget) with fix; resumed correctly at thread 5 batch 12.
