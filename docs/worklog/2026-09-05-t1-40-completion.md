# 2026-09-05 — t1–40 run completion report

**The run completed.** Supervisor log: attempt 1 (01:04–03:08) died on the FTS apostrophe bug; attempt 2 (03:38 onward, fixed code, restored checkpoint) completed at 13:52 — `run completed`, exit 0.

## Output

- `data/annotator-t1-40.db`: **416 batches, 177 entries, 23 shadow deferrals, 0 recorded failure diagnostics** in the completing attempt.
- `verify`: **exit 0, all invariants hold** (all evidence quotes re-verified against the corpus).
- Recordings: `data/recordings/t1-40.jsonl` (L4 replay source).

- Coverage evidence: `COUNT(DISTINCT story_log.thread_id) = 40` (all requested threads have gists); final checkpoint `32266754` = last ID in `threads-first-40.json`.

## Metrics

| Metric | Value | Note |
|--------|-------|------|
| Entries / 1k story posts | **90** (177 over 1,962 posts) | vs 444/1k in the 2-thread smoke — but confounded: threads 1–2 are exposition-densest and this pass ran with the epistemic prompt + salience serving. Don't read causality into the ratio alone. |
| Gold coverage — denominators | 806 raw link occurrences; **218 unique (namespace, slug) pairs**; 2 `meta:` links excluded (`soma`, `shadowrunning` — QM/meta, correctly out of glossary scope) → **216 in-scope** | 218 pairs − 201 unique slugs = 17 extra pairs from cross-namespace slug collisions (188 slugs appear in 1 namespace, 11 in 2, 2 in 4) — pairs are the canonical key |
| — exact normalized surface/alias matches (namespace unresolved) | **65/216 (30%)** | entry term or registered alias normalizes to the slug; our DB stores no wiki namespace, so same-slug-different-namespace pairs can't be disambiguated from the DB alone |
| — fuzzy token-subset candidates | +81 (→ 146/216, 68%) | **heuristic diagnostic only**: slug tokens ⊆ entry tokens or vice versa; many are true aliases (their `centurion-armor` = our "Rhynian Centurion Armor"), some are coincidental. Review before treating as coverage. |
| — missed entirely | **70/216 (32%)** | the diagnostic list below |
| Deferred log | 23 candidates | for researcher review |

## Missed entities (70) — the diagnostic list

- **Characters are the weakest class**: `mik` (the protagonist, *still*), `suresh`, `mazhar`, `karim`, `demetrios`, `onur`, `lord-hodei`, `maximilien`, `old-sam`, `papa-dracolich`, `the-three`, `portal-guardian`, `void-messenger`, `zahak` (the egg), `northern-elf-warlord`, `entity`.
- **Books almost entirely missing** (Matt's predicted hard class): `excidium`, `ninth-barrier`, `leaping-scroll`, `beginners-guide-*`, `empire-rise-of-civilization`, plus `rostams-journal`, `fire-priest-journal`, `eshnuk-book-of-rituals` variants.
- **Magic taxonomy thin**: `levels`, `master`/`grandmastery`, `enchanting:golem[s]`, `enchanting:runes`, `enchanting:pocket-dimension`, `metamagic:leaping`/`manipulation`, `techniques:defibrilation`, `ice`, `nature`, `mechanics:insanity`, `mechanics:status`, `traits:silver-tongued`/`2spoopy`.
- **Objects**: `brooch`, `oud`, `miks-spear`, `shockwave-sword`, `gladius`, `ring-of-sanity`, `computor`, `purification-chamber`, `void_generators`, `money`, `hammock`.
- **Locations/misc**: `khmedi`, `mundus`/`umndus` (duplicate-slug pair), `spirit[s]`, `sanity`, `spirit-geis`, `strings-of-reality`, `prthvi`, `srsti`, `voces-mysticae`, `dwoveth`, `bhataki`, `dragon-cult`, `siege` (culture+game), `arak`, `human`.
- **Correctly out of scope** (meta links): `meta:soma`, `meta:shadowrunning` — the QM and a joke page; a glossary shouldn't have these.

## Reading the misses

Three causes, in likely order of contribution: (1) **person-name under-detection** — the prompt's term classes emphasize *jargon*; main-cast names read as "just names" to the model; (2) **title-like objects** (books) read as texture — the exact class Matt predicted, needs the `book`/document tag prior or prompt callout; (3) **taxonomy nodes** (levels, effects) that the annotator folds into parents but the wiki splits out — the sub-entry split rule's absence in the write path.

## Next

- Prompt revision for the next pass (admission criterion OR-form + protagonist/books callouts) — in `docs/plan/aspirations.md`.
- Researcher tier can close several classes post-hoc (reread linking + reclamation) on this DB.
- The 9 duplicate story gists from the killed attempt-2 restart remain in the log (documented, harmless).
