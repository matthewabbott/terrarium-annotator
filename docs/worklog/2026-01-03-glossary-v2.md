# 2026-01-03 - F10: Glossary v2 (Split Glossary/Codex)

## Objective

Redesign the glossary system to distinguish between two types of entries:
1. **Glossary**: Jargon/vocabulary terms that would confuse readers
2. **Codex**: Wiki-style entries for named entities

## Context

Quality issues identified in existing glossary (656 entries):
- Trivial entries: "75 silver transaction", "60% refund policy"
- Near-duplicates: "Vys Array" vs "Vys Array Transformation"
- No distinction between vocabulary (for embedding tuning) and wiki content

Critical problem: **Semantic jargon** - common English words with domain-specific meanings:
- "soul", "husk", "shell", "shard" in fantasy settings
- "egress", "redlines", "chartfield string" in building code
- These won't be flagged by dictionary checks but are exactly what we need for embedding vocabulary tuning

## Work Done

### Phase 1: Prompt Engineering
- [x] Updated `TOOL_SYSTEM_PROMPT` to distinguish glossary from codex
- [x] Added explicit INCLUDE/EXCLUDE criteria for each type
- [x] Emphasized semantic jargon detection (capitalized common words)

### Phase 2: Split Tools
- [x] Migration 007: Added `entry_type` column to `glossary_entry`
- [x] Updated `GlossaryStore` with entry_type in create/search/get methods
- [x] Added `EntryType` type alias and export
- [x] Added 4 codex tool schemas (search, create, update, delete)
- [x] Updated `ToolDispatcher` with codex handlers
- [x] Added `sweep_mode` parameter to `get_all_tool_schemas()`

### Phase 3: Auto-Detection with NLTK
- [x] Created `terrarium_annotator.detection` module
- [x] `NovelTermDetector` using NLTK words corpus
- [x] Detects three categories:
  - `<novel>`: Made-up words not in dictionary
  - `<capitalized_common>`: English words capitalized mid-sentence (semantic jargon)
  - `<existing_glossary>`: Terms already tracked
- [x] `format_detected_terms_xml()` for agent context
- [x] 15 tests for detection module

### Phase 4: Sweep Modes CLI
- [x] Added `--sweep-mode` argument (glossary/codex/both)
- [x] Added `GLOSSARY_SWEEP_PROMPT` for jargon-focused sweeps
- [x] Added `CODEX_SWEEP_PROMPT` for wiki-focused sweeps
- [x] Added `get_system_prompt()` function
- [x] Updated runner to use sweep_mode for tools and prompts

## Decisions Made

### Decision: Same table vs separate tables for glossary/codex
**Options considered**:
1. Separate `glossary_entry` and `codex_entry` tables
2. Same table with `entry_type` column
**Chose**: Option 2 - same table with entry_type column
**Rationale**: Simpler migration, shared search/FTS, entries can be reclassified

### Decision: Dictionary for novel term detection
**Options considered**:
1. NLTK words corpus
2. Custom wordlist
3. External API (dictionary.com, etc.)
**Chose**: Option 1 - NLTK words corpus
**Rationale**: Well-maintained, 236K words, no API latency, offline-capable

### Decision: Fresh database for v2
**Chose**: Start fresh database, no migration of existing 656 entries
**Rationale**: Cleaner approach, existing entries have quality issues anyway

## Configuration Changes

New CLI argument:
```bash
python -m terrarium_annotator.cli run --sweep-mode glossary  # Jargon only
python -m terrarium_annotator.cli run --sweep-mode codex     # Wiki only
python -m terrarium_annotator.cli run --sweep-mode both      # Default
```

New dependency:
```
nltk>=3.8.0
```

## Files Changed

### New Files
- `src/terrarium_annotator/detection/__init__.py`
- `src/terrarium_annotator/detection/detector.py`
- `tests/test_detection.py`

### Modified Files
- `src/terrarium_annotator/context/prompts.py` - Mode-specific prompts
- `src/terrarium_annotator/context/__init__.py` - Export get_system_prompt
- `src/terrarium_annotator/storage/migrations.py` - Migration 007
- `src/terrarium_annotator/storage/glossary.py` - entry_type support
- `src/terrarium_annotator/storage/__init__.py` - EntryType export
- `src/terrarium_annotator/tools/schemas.py` - Codex schemas, sweep_mode
- `src/terrarium_annotator/tools/dispatcher.py` - Codex handlers
- `src/terrarium_annotator/tools/glossary_tools.py` - entry_type param
- `src/terrarium_annotator/runner.py` - sweep_mode integration
- `src/terrarium_annotator/cli.py` - --sweep-mode arg
- `requirements.txt` - nltk dependency
- `tests/test_tools.py` - Updated for new tool count
- `tests/test_integration.py` - Updated emergency threshold

## Test Results

All 293 tests pass:
- 15 detection tests (new)
- 38 tools tests (updated)
- 22 runner tests
- 218 other tests

## Open Questions

- Should detected_terms XML be included in scene context automatically?
  - Currently just the module exists; not wired into runner yet
- Token budget for detected_terms section?

## Next Steps

1. Wire detection into runner (add to scene context)
2. Test sweep modes with live corpus
3. Tune novel term detection thresholds if needed

---
*Agent: Claude Opus 4.5*
*Duration: ~1 hour*
