# Handover — 2026-09-12 (session start point for next agent)

Read in order: `SPEC.md` → `docs/design/v2-architecture.md` → `docs/design/critic-salience-epistemics.md` → `docs/plan/aspirations.md` → this file → `docs/worklog/2026-09-12-full-run-stocktake.md`.

## Where we are

- **System works end-to-end**: annotator reads Banished Quest (278 threads — 276 with `op_post` tags, 2 without) via kimi-k2.5 over omp RPC, writes quote-gated glossary entries; researcher does top-down corpus-wide alias/dedupe/retitle work; verifier checks every stored quote against the corpus. 175 tests green on `feature/v2-foundation` (pushed).
- **Full-quest run paused by Matt at quota stop**: `data/annotator-full.db` covers threads 1–24 + part of 25 (~9% of corpus), 455 entries, checkpoint 31411898:4. Restart ONLY on Matt's explicit go-ahead.
- **Key findings**: relaxed prompt fixed the baseline's holes (books, taxonomy, characters) at ~3× tool calls/batch, ~3× prompt chars/batch (measured per-batch; total quota multiplier is UNATTRIBUTED). Shadow gate flagged 239/455 candidates — a candidate review set, NOT a junk queue (~half are valid per calibration). Cost mechanisms measured: turns × cold context × growing cards.
- **Baseline comparisons**: `data/annotator-t1-40.db` (threads 1–40, 177 entries, researcher-passed, verify-clean) is the quality reference.

## Next session's work, in order

1. **Usage telemetry — GATED TASK** (acceptance criteria in `docs/plan/aspirations.md` under "NEXT: usage telemetry"; either met, or stop and report the blocker). Provider-neutral wrapper, char-based fallback counters, provider `usage` only when reported (null never invented). Zero behavior change. L0 tests + merge bar.
2. **Bounded instrumented adjudication pass** (`docs/worklog/2026-09-12-full-run-stocktake.md` rec #1): researcher quote-audits ~40–60 of the 239 flagged candidates in `annotator-full.db`; measure shadow-flag precision per class + bounded baseline cost via the new telemetry. Do NOT bulk-prune.
3. Then, with that data: the prompt ladder / Deepseek v4.1 Flash onboarding (`docs/design/prompt-laddering.md`), and Matt's call on restarting the full run.

## Constraints

- `banished.db` is read-only, always. No bulk deletions ever — demote to graveyard, human queue for merges/deletes.
- 3-failure stop conditions everywhere; merge bar (`pytest`, `ruff check`, `ruff format --check`) green per commit; push to origin when committing.
- Advisory review is active and valuable — answer every advisory item with evidence before claiming done.
- Matt's review queue (in `data/annotator-t1-40.db`): 5 merge proposals + the `archeota` alias on Weird Sphere (no alias-deletion tool exists yet).
