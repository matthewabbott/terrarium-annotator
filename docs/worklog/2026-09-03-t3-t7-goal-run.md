# 2026-09-03 — T3–T7 goal-mode run

**Author**: omp agent session (driver: Matt-launched, goal mode: T3–T7)

## Scope

Execute T3–T7 per `docs/plan/v2-foundation.md` under the session goal: merge bar green per task, one commit per task, design deviations documented in the same commit.


## Notes

- T3 complete: `glossary/` (models, store, `__init__`) + `tests/test_glossary.py` (21 tests). Real bugs caught by L0: `add_alias` re-checked the entry's own term as a collision (split into `_check_surface_free(key, allow_entry_id)`); alias registration originally recorded no provenance — now quote-gated like other writes, with an `entry_source` row (NULL revision_id).
- CorpusReader gained `post_body(post_id)` (production wiring for the gate); reader.py rewritten cleanly after an edit-tool mishap; T1 tests re-verified.
- Merge bar: 52 passed, ruff check + format clean.
- Next: T4 injection layer.
- T4 complete: `inject/select.py` (whole-word case-sensitive triggers, gloss-only recursion ≤1, budget with round0>round1>recency>cheap priority) + `tests/test_injection.py` (12 tests). Review caught recursion scanning displayed terms instead of glosses; fixed with adversarial regression test. Merge bar: 64 passed.
- Next: T5 LLM client seam.
- T5 complete: `llm/` (base protocol + parse_choice, OpenAICompatibleClient with retry/backoff and endpoint normalization for `/v1` bases, ScriptedModel, RecordingClient/ReplayClient for L4) + `tests/test_llm.py` (15 tests incl. stub-server retry/4xx/drift cases). `requests` re-added as a dependency. Merge bar: 79 passed.
- Next: T6 runner + tools.
- Storage boundary resolved per architecture §7: `state.py connect_annotator_db()` — one shared connection for story log + glossary + transcript + run_state; both stores accept a shared connection (close() only for owners).
- T6 complete: `tools/dispatch.py` (7 tools, schemas, domain errors → error payloads) + `runner.py` + `state.py` + `tests/test_runner.py` (12 tests, full L1 set from dev-verification.md). Bugs caught pre-commit: dead `if False` assistant append (invalid tool history); resume flag never clearing past a fully-checkpointed thread (rewrote run() index-based); test-authoring slips (fixture text from T2, batch_size kwarg collision, oversized-script arity) all fixed with the suite green. Merge bar: 91 passed.
- Next: T7 verify CLI.
- T7 complete: `verify.py` (8 invariant checks: quote validity, provenance coverage, backlink integrity, revision links, tree alignment, gist hygiene, run-state, budget compliance vs recorded config) + `cli.py` (`terrarium-annotator verify`) + `tests/test_verify.py` (13 tests). Runner now records each batch's assembled user message and run config (run_meta) so budget compliance is checkable post-hoc. Real console-script smoke: seeded scripted run → `verify` exits 0. Merge bar: 104 passed.
- Goal run complete: T3–T7 all done. 104 tests total. Remaining: G1–G4 (Matt), T8 (deferred).
- Post-goal fix (review): `load_run_state` now filters by `pass_id` — a new pass no longer resumes from (and skips work covered by) another pass's checkpoint. Regression test `test_new_pass_id_starts_fresh`. 105 tests.
- Quota/model probe (Matt-authorized, 3 tiny prompts total): **K2.5, K2.7 (`kimi-for-coding`), and K3 are all entitled** — each completed a real prompt via RPC (`set_model` + prompt → "OK"). No `kimi-k2.6` exists in the registry. Quota structure: shared account pool, 5h + 7-day windows, integer-percent, unit "unknown" (`omp usage --json`; endpoint `api.kimi.com/coding/v1/usages`); no per-model breakdown anywhere (omp's `model_usage` table tracks only last-used timestamps). Three tiny prompts moved neither gauge → **per-model quota cost is not resolvable at available granularity**; quota burn is minimized by fewer requests/retries, not model choice. Recommendation: pick on L3 smoke quality between K2.5 and K2.7 (both 262k ctx); K3's 1M window is unnecessary given our per-batch budgets.
- L3 smoke complete (goal run): thread filter (`only_threads`, chronological, checkpoint-ignoring, unknown-ID rejection), OmpRpcClient (`--no-tools` invariant enforced at construction + preflight; reader-thread timeout; text tool-call convention), `run` CLI with injectable client factory. 105 → 128 tests across these commits.
- Smoke: 2 threads, 90 story posts, 18 batches, kimi-k2.5, ~37 min. verify exit 0. Crash bug found+fixed (duplicate alias IntegrityError). Quota 7d 23→24%, 5h 56→64%.
- **Quality report: `docs/worklog/2026-09-03-l3-smoke-report.md` (40 entries with glosses + quotes) — 444 entries/1k posts, junk tail present, Aleamond duplicate pair, Vys gloss-narrowing issue. Continuation gated on Matt's review.**
