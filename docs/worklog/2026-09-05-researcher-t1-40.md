# 2026-09-05 — Researcher pass on t1–40: before/after comparison

**Setup**: `Researcher` (top-down, corpus-wide search incl. beyond read position — Matt's call) over `data/annotator-t1-40.db`, kimi-k2.5, focus "aliases and best-fit retitles and dedupe proposals". Session: 26 min, exit 0. Recording: `data/recordings/research-t1-40-v2.jsonl`.

## What it did

- **+24 aliases** (290 → 314), incl. the two highest-profile linkability holes: `mik` → Mikhael Abdul-Hakim, `suresh` → Archmagos Megalos.
- **12 revisions** under pass_id `research`.
- **5 merge proposals queued** (none applied — human queue):
  1. Archmagos ↔ Archmagos Megalos (same person)
  2. Archmagos ↔ Archmagos Megalos (duplicate proposal, different evidence)
  3. generator ↔ Void Generator (same device class)
  4. Void Generator ↔ Rhynian Lantern (Fulvia classifies lanterns as minor void generators)
  5. **Dux Cyprian ↔ Mikhael Abdul-Hakim** (same referent — it found the story's alias identity on its own)
- **Human-review items it flagged**: remove the `archeota` alias from Weird Sphere (bad alias; no alias-deletion tool exists — noted as a tooling gap), adjudicate merge queues 1–5.

## Coverage effect (the measurable point of the exercise)

| Metric | Before | After |
|--------|--------|-------|
| Exact surface/alias matches vs gold (216 in-scope) | 65 (30%) | **67 (31%)** |
| + fuzzy token-subset candidates (diagnostic only) | 146 (68%) | 149 (69%) |

Modest numerically — but the two gained exact matches are the single highest-frequency links in the corpus (`mik` appears on all 38 pages). The remaining character/object misses (`mazhar`, `demetrios`, `karim`, `brooch`, `oud`, `shockwave-sword`, …) need entry *creation* via corpus investigation, which the 40-round session didn't reach; a longer/focused session or the next annotator pass should get them.

## Process notes (the run itself taught)

- First live researcher session did zero work: the model announced intent without tool calls, and `research()` accepted it as final. Fixed: bounded nudge (≤2) on pre-work announcements + a never-announce prompt rule + a ScriptedModel recording-fidelity fix (deep-copy per call; reference-aliasing was corrupting request logs) — the last being a real L1-infrastructure bug.
- Two known tooling gaps to close: alias deletion (for bad aliases like `archeota` on Weird Sphere) and merge-queue accept/reject ops for Matt's review flow.


## Post-run verification (added 2026-09-05, review gap)

`terrarium-annotator verify --corpus-db banished.db --annotator-db data/annotator-t1-40.db` → exit 0, all invariants hold, after the researcher pass. Alias baseline evidence: the v1 recording (`data/recordings/research-t1-40.jsonl`) shows zero tool calls, so 290 aliases at that point = pre-researcher count; 314 after = +24 from the researcher session.